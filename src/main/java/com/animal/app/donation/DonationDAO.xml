<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <!-- namespace의 이름은 사용하려는 DAO의 풀패키지명과 동일하게 작성 -->
  <!-- DonationDAO 인터페이스와 연결 -->
  <mapper namespace="com.animal.app.donation.DonationDAO">
  
  <!-- Donation 테이블에 insert -->
  <insert id="insertDonation" parameterType="DonationVO">
  	INSERT INTO DONATION
  	(MEMBER_NO, DOG_NO, TID, AMOUNT, STATUS, APROVED_AT)
  	VALUES (#{memberNo}, #{dogNo}, #{tid}, #{amount}, #{status}, #{aprovedAt})
  </insert>
  
  <!-- 유기견 별 총 후원금 select -->
  <select id="selectAmount" parameterType="long" resultType="int">
  	SELECT SUM(AMOUNT)
  	FROM DONATION
  	WHERE DOG_NO = #{dogNo}
  </select>
  
  
  </mapper>